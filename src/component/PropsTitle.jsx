

const PropsTitle = (props) => {
    return (
        <div>
            <p title="Hello Humayan"></p>
            <h1>{props.title}</h1>
            <h1>{props.des}</h1>
        </div>
    );
};

export default PropsTitle;